import { Meta, Story } from "@storybook/addon-docs/blocks";
import colors from "../../colors.json";
import HBCardFront from "./HBCardFront";

<Meta
  title="HBCardFront"
  component={HBCardFront}
  argTypes={{
    suit: {
      control: {
        type: "select",
        options: colors.map((c) => c.name),
      },
    },
    rank: {
      control: {
        type: "number",
        min: 1,
        max: 5,
        step: 1,
      },
    },
    borderOverride: { control: "color" },
    parameters: {
      layout: "centered",
    },
  }}
/>

export const Template = (props) => <HBCardFront height="200px" {...props} />;

<Story name="HBCardFront" args={{ rank: 1, suit: "Red" }}>
  {Template.bind({})}
</Story>
